# ─────────────────────────────────────────────────────────────────────────────
# .env.example — copy this to .env and fill in your values.
#
#   cp .env.example .env
#
# The .env file is gitignored. Before running any terraform command, source
# the loader script so these values are exported as TF_VAR_* variables that
# Terraform picks up automatically:
#
#   source scripts/load-env.sh
# ─────────────────────────────────────────────────────────────────────────────

# Email address that receives CloudWatch alarm notifications via SNS.
# Required — Terraform will error if this is not set.
ALERT_EMAIL=your-email@example.com

# AWS CLI profile to use. Optional — omit or leave blank to use the
# default credential chain (env vars, instance profile, etc.).
# AWS_PROFILE=
